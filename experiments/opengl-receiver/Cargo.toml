[package]
name = "opengl-receiver"
version = "0.1.0"
edition = "2024"

[dependencies]
glfw = "0.60.0"
nix = { version = "0.30.1", features = ["socket", "uio"] }
[build-dependencies]
gl_generator = "0.14"
